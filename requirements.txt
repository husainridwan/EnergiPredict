# EnergiPredict — runtime dependencies (serving + training)
#
# The model libraries are runtime dependencies, not just training ones: the
# deployed stacking ensemble unpickles XGBoost/LightGBM/CatBoost estimators,
# so the serving process needs them importable to call .predict().
#
# For exact reproducibility use requirements.lock.txt instead.
# For the notebook and plots, also install requirements-dev.txt.

# --- web ---
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9      # multipart/form-data parsing for CSV upload

# --- data ---
numpy>=2.0
pandas>=2.2

# --- models ---
scikit-learn>=1.5
xgboost>=2.1
lightgbm>=4.5
catboost>=1.2
joblib>=1.4
